//rule created automatically with crits intelligence
rule TEMPLATE_RULE_NAME
{
    meta:
        author = "CRITS Export"
        mime_type = "text/rtf,application/rtf,application/x-rtf,text/richtext,application/rtf,text/richtext"
    strings:
TEMPLATE_STRINGS
    condition:
        any of them
}
